# Soulforge — UI 设计规范

> 设计目标：**桌面单页 Web 应用**，浏览器全屏使用，**三栏布局**（Agent 树 + 文件树 + 编辑器）。
> 类似 VSCode / Obsidian 的工作流气质，但更轻。
> 用户：老板（单人本地自托管）。

---

## 一、整体布局

### 1.1 三栏布局（桌面 ≥1280px）

```
┌──────────────────────────────────────────────────────────────────────────────┐
│ TopBar   Soulforge · 主 Agent: main · 6 个 Agent · 12 个警告   [搜索] [⚙]  │
├────────────┬──────────────────────────┬─────────────────────────────────────┤
│            │                          │                                     │
│  Agent 树  │  文件树                  │  编辑器 / 预览                      │
│            │                          │                                     │
│  ▾ main    │  ▾ CORE                  │  ┌─────────────────────────────┐    │
│    • SOUL  │    SOUL.md              │  │  # SOUL.md                  │    │
│    • USER  │    AGENTS.md            │  │                             │    │
│    • MEMORY│    IDENTITY.md           │  │  [Monaco Editor]            │    │
│  ▸ xiaowei │  ▾ MEMORY                │  │                             │    │
│  ▸ xiaoxi  │    2026-08-05.md        │  │                             │    │
│  ▸ caicai  │    2026-08-04.md        │  │                             │    │
│            │                          │  └─────────────────────────────┘    │
│            │                          │  [保存]  [历史]  [检查]  [导出]    │
│            │                          │                                     │
├────────────┴──────────────────────────┴─────────────────────────────────────┤
│ StatusBar  ● 连接到 6 个 Agent · 索引 128 个文件 · 上次扫描: 2 分钟前        │
└──────────────────────────────────────────────────────────────────────────────┘
```

### 1.2 响应式断点

| 断点 | 布局 |
|---|---|
| ≥1280px | 三栏全显 |
| 768-1279px | 左栏可折叠（侧滑），中右两栏 |
| <768px | **不支持**（弹出提示"请用 ≥1280px 屏幕访问"） |

**理由**：老板是桌面端用户，移动端不在 MVP 范围。

---

## 二、窗口尺寸

| 属性 | 值 |
|---|---|
| 默认窗口宽 | 全屏（无固定） |
| 最小窗口宽 | 1280px（强制） |
| 默认窗口高 | 全屏（无固定） |
| TopBar 高 | 48px |
| StatusBar 高 | 28px |
| 左栏（Agent 树） | 240px（可拖拽 180-360px） |
| 中栏（文件树） | 280px（可拖拽 200-400px） |
| 右栏（编辑器） | 自适应剩余 |

---

## 三、配色 & 主题

### 3.1 双主题

| 主题 | 触发 |
|---|---|
| **浅色**（默认） | 跟随系统 |
| **深色** | 跟随系统 / 顶部开关手动切换 |

### 3.2 主色板（参考 VSCode + Obsidian）

| Token | 浅色 | 深色 | 用途 |
|---|---|---|---|
| `--bg-base` | `#FAFAFA` | `#1E1E1E` | 主背景 |
| `--bg-elevated` | `#FFFFFF` | `#252526` | 卡片/侧栏 |
| `--bg-hover` | `#F0F0F0` | `#2A2D2E` | hover 态 |
| `--border` | `#E5E5E5` | `#3C3C3C` | 边框/分割线 |
| `--text-primary` | `#1F1F1F` | `#D4D4D4` | 正文 |
| `--text-secondary` | `#6B6B6B` | `#858585` | 次要文字 |
| `--accent` | `#3B82F6` | `#60A5FA` | 主交互（按钮/链接） |
| `--accent-hover` | `#2563EB` | `#3B82F6` | 主交互 hover |
| `--success` | `#10B981` | `#34D399` | 成功提示 |
| `--warning` | `#F59E0B` | `#FBBF24` | 警告 |
| `--danger` | `#EF4444` | `#F87171` | 危险操作 |
| `--code-bg` | `#F4F4F5` | `#1E1E1E` | 代码块背景 |

### 3.3 语义色

- ✅ 绿色 = 保存成功 / 健康
- ⚠️ 黄色 = 警告（lint 警告 / 备份提示）
- ❌ 红色 = 危险（删除 / 覆盖冲突）
- 🔵 蓝色 = 信息 / 链接

---

## 四、组件库

**用 shadcn/ui**（复制粘贴式，不锁版本）：

| 用途 | 组件 |
|---|---|
| 按钮 | Button（default / secondary / destructive / ghost） |
| 弹窗 | Dialog（确认对话框） |
| Toast | Sonner（顶部通知） |
| 下拉 | DropdownMenu |
| 文件树 | 自己封装（shadcn 没原生） |
| 标签 | Tabs / Badge |
| 输入框 | Input / Textarea |
| 选择器 | Select（多选用 Combobox） |
| Diff 渲染 | diff2html |

---

## 五、交互细节

### 5.1 文件编辑

- **保存快捷键**：`Cmd/Ctrl + S`
- **关闭未保存提示**：右上角显示「● 未保存」标记，关闭时弹确认
- **自动保存**：默认 **关闭**（老板手动控制，避免误操作）
- **保存前校验**：
  - 单文件 > 50KB → 提示「文件过大，确认保存？」
  - 内容为空 → 提示「将清空文件，确认？」

### 5.2 危险操作确认

**所有危险操作必须弹对话框二次确认**，不能只靠 Toast：

| 操作 | 确认级别 |
|---|---|
| 编辑单文件保存 | Toast 即可 |
| 跨 Agent 编辑保存 | Dialog 确认（显示影响 Agent 列表） |
| 导入 tar.gz | Dialog 确认（显示冲突文件） |
| 回滚 | Dialog 确认（显示当前 vs 历史 diff） |
| 删除备份 | Dialog 确认 + 输入「确认删除」 |

### 5.3 Lint 警告显示

- 文件树里有警告的文件 → 文件名右边小红点 ●
- 编辑器右侧 → 警告图标，点击跳到对应行
- 警告悬浮 → 显示规则名 + 建议改法

### 5.4 跨 Agent 编辑模式

切换到「跨 Agent 编辑」时：

- 顶部 TopBar 变红 + 显示「⚠ 跨 Agent 编辑模式」
- 编辑器右上角显示「影响 Agent：main, xiaowei-ops, xiaoxi-lawyer（3）」
- 顶部实时显示当前内容差异度（相似度条）

### 5.5 搜索框

- 顶部 TopBar 中央 / Cmd+K 唤起命令面板
- 支持：跨 Agent / 限定 Agent / 限定文件类型
- 结果列表显示：`Agent ID · 文件路径 · 匹配行（高亮）`
- 点击结果 → 跳到对应文件 + 滚动到匹配行

---

## 六、动效

| 场景 | 动效 |
|---|---|
| 页面切换 | 无（保持工作流连贯） |
| Toast 出现 | 顶部滑入，3 秒后自动消失 |
| 危险操作确认 | Dialog 缩放淡入 |
| Lint 警告出现 | 右侧滑入 |
| 文件树展开/收起 | 200ms ease |

**整体原则**：**少动效，专注工作流**。参考 Obsidian 哲学。

---

## 七、空状态 & 加载状态

### 7.1 加载状态

- 第一次打开 → 显示「正在发现 Agent...」+ 进度条
- 搜索时 → 输入框右侧转圈
- 保存时 → 按钮变 spinner

### 7.2 空状态

- 无 Agent → 显示「未找到任何 Agent，请检查 openclaw.json 配置」+ 「重新扫描」按钮
- 无文件 → 显示「这个 Agent 的 workspace 是空的」
- 无搜索结果 → 显示「没有匹配的内容」

---

## 八、可访问性

- **键盘可操作**：所有按钮支持 Tab 聚焦 + Enter 触发
- **快捷键**：

| 快捷键 | 功能 |
|---|---|
| `Cmd/Ctrl + S` | 保存当前编辑 |
| `Cmd/Ctrl + K` | 命令面板（搜索 / 跳） |
| `Cmd/Ctrl + B` | 折叠左栏 Agent 树 |
| `Cmd/Ctrl + Shift + E` | 跨 Agent 编辑模式 |
| `Esc` | 关闭弹窗 / 退出跨 Agent 模式 |

- **字体**：`Inter`（主）+ `JetBrains Mono`（代码）
- **字号**：正文 14px / 次要 12px / 标题 16-20px

---

## 九、视觉参考

- **主参考**：VSCode（三栏布局）
- **次参考**：Obsidian（文件树 + Markdown 编辑）
- **不要参考**：Notion（太花哨）、飞书文档（功能太杂）

**老板审美倾向**：简洁、克制、专注。

---

## 十、文案规范

- **按钮文案**：用动词 + 名词（"保存"/"删除备份"/"跨 Agent 同步"）
- **不用 emoji 按钮文案**（按钮上不要 emoji，文件图标可以用）
- **错误信息**：说人话 + 给解决方案（"保存失败：权限不足 → 请检查 workspace 路径权限"）
- **不堆技术 jargon**（不写 "500 Internal Server Error"，写 "保存失败：服务器内部错误"）